# ==============================================
# PRODUCTION ENVIRONMENT VARIABLES
# ==============================================
# IMPORTANT: Copy this file to .env and fill in the actual values
# NEVER commit .env file to version control!

# ==============================================
# DATABASE CONFIGURATION
# ==============================================
POSTGRES_DB=shipment_tracking_prod
POSTGRES_USER=shipmentuser
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# ==============================================
# BACKEND (.NET API) CONFIGURATION
# ==============================================

# Database Connection
DB_HOST=postgres
DB_PORT=5432
DB_NAME=shipment_tracking_prod
DB_USER=shipmentuser
DB_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD

# JWT Authentication Configuration
# IMPORTANT: Generate with: openssl rand -base64 48
JWT__SECRETKEY=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_48
JWT__ISSUER=VertexTransportAPI
JWT__AUDIENCE=VertexTransportClient
JWT__ACCESSTOKENMINUTES=60
JWT__REFRESHTOKENDAYS=7

# Email Configuration (choose one)
# Option 1: Resend.com (recommended for production)
RESEND__APIKEY=CHANGE_ME_YOUR_RESEND_API_KEY
RESEND__FROMEMAIL=info@vertextransinc.com
RESEND__FROMNAME=Vertex Transport Inc

# Option 2: SMTP (alternative)
SMTP__HOST=smtp.example.com
SMTP__PORT=587
SMTP__USESSL=true
SMTP__FROMEMAIL=noreply@vertextransinc.com
SMTP__FROMNAME=Vertex Transport Inc

# Admin User (initial setup - change these!)
ADMINUSER__EMAIL=admin@vertextransinc.com
ADMINUSER__PASSWORD=CHANGE_ME_STRONG_ADMIN_PASSWORD
ADMINUSER__FIRSTNAME=Admin
ADMINUSER__LASTNAME=User

# CORS Allowed Origins (comma-separated)
CORS_ALLOWED_ORIGINS=https://vertextransinc.com,https://www.vertextransinc.com

# ==============================================
# FRONTEND (Next.js) CONFIGURATION
# ==============================================

# API URLs
NEXT_PUBLIC_API_URL=https://vertextransinc.com/api
API_INTERNAL_URL=http://backend:8080/api

# Site URL
NEXT_PUBLIC_SITE_URL=https://vertextransinc.com

# Environment
NODE_ENV=production

# ==============================================
# DEPLOYMENT CONFIGURATION
# ==============================================

# Domain Name (used for SSL certificates and nginx configuration)
DOMAIN_NAME=vertextransinc.com

# Let's Encrypt Email (for SSL certificates)
LETSENCRYPT_EMAIL=admin@vertextransinc.com

# ==============================================
# OPTIONAL: MONITORING & ANALYTICS
# ==============================================

# Sentry (Error Tracking)
# NEXT_PUBLIC_SENTRY_DSN=your_sentry_dsn
# SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ==============================================
# SECURITY NOTES
# ==============================================
# 1. Generate strong passwords: Use a password manager or run:
#    openssl rand -base64 32
#
# 2. JWT Secret must be at least 32 characters. Generate with:
#    openssl rand -base64 48
#
# 3. Rotate secrets regularly (every 90 days recommended)
#
# 4. Use environment-specific .env files:
#    - .env.local (local development)
#    - .env.staging (staging environment)
#    - .env.production (production environment)
#
# 5. Store production secrets in a secrets manager:
#    - Azure Key Vault
#    - AWS Secrets Manager
#    - HashiCorp Vault
